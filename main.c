#include <stdio.h>
#include <stdlib.h>
#include "teste.h"
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(void) {
	int somado = soma(1,2);
	printf("somou %d",somado);
	return 0;
}
